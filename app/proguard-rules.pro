# Add narrowly scoped R8/ProGuard rules only when a dependency or integration requires them.
# Do not add broad keep rules until the HubSpot/WebView implementation and release-build evidence are available.
